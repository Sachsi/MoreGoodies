cmake_minimum_required(VERSION 3.25)
project(StarbuzzCoffee)
set(CMAKE_CXX_STANDARD 17)

include_directories(
    ${PROJECT_SOURCE_DIR}/inc
    ${PROJECT_SOURCE_DIR}/src
)

file(GLOB SRCS
    "${PROJECT_SOURCE_DIR}/inc/*"
    "${PROJECT_SOURCE_DIR}/src/*.cpp"
)
add_executable(StarbuzzCoffee ${SRCS})

include(CTest)
enable_testing()